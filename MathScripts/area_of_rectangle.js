// These are the dimensions of a rectangle, the dimensions are made up by me

const length = 20.5;
const width = 9.5;

// In this portion of my code I used a variable declared by let to show the formula I came up with to output the area of a triangle.
let areaOfRectangle = length * width;

console.log("The area of the rectangle is: " + areaOfRectangle);